<?php

return [

    // The `web` guard (session-based) is used exclusively by the admin
    // dashboard and is restricted to role in [admin, dispatcher] via the
    // EnsureIsAdmin middleware. The mobile API uses the `sanctum` guard,
    // wired in routes/api.php via the `auth:sanctum` middleware.
    'defaults' => [
        'guard' => 'web',
        'passwords' => 'users',
    ],

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],

        'sanctum' => [
            'driver' => 'sanctum',
            'provider' => 'users',
        ],
    ],

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => App\Models\User::class,
        ],
    ],

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    'password_timeout' => 10800,

];
